"use client";

import Link from "next/link";
import {useParams,useRouter} from "next/navigation";
import {useEffect,useMemo,useState} from "react";
import {api} from "../../../../lib/api";

type Member={id:string;agent:string;agent_name:string;role:string;priority:number;can_delegate:boolean;enabled:boolean};
type Team={id:string;project:string|null;name:string;objective:string;kind:string;director:string;director_name:string;active:boolean;max_cost_rub_per_run:string;members:Member[]};
type Step={id:string;sequence:number;title:string;action_type:string;state:string;agent_name:string;public_log:string;cost_rub:string};
type Approval={id:string;title:string;status:string};
type Run={id:string;state:string;objective:string;cost_actual_rub:string;step_count:number;tool_call_count:number;created_at:string;steps:Step[];approvals:Approval[];output_payload?:{repository?:string;working_branch?:string;text?:string;applied_changes?:Array<{path?:string;operation?:string}>}};
type Binding={id:string;full_name:string;default_branch:string;write_enabled:boolean;private:boolean};
type ReadinessAction={code:string;label:string;href?:string};
type TeamReadiness={ready:boolean;checks:Record<string,boolean>;blockers:string[];warnings:string[];actions:ReadinessAction[];models:{role:string;level:string;model:string}[]};

const stateLabel:Record<string,string>={queued:"В очереди",planning:"Планирование",running:"Выполняется",waiting_tool:"Ожидает инструмент",waiting_approval:"Нужно подтверждение",reviewing:"Проверка",completed:"Готово",failed:"Ошибка",canceled:"Отменён",budget_exceeded:"Лимит бюджета"};
const stageDefs=[
 {key:"architecture",label:"Architecture",roles:["Architecture","Engineering Director"],types:["github_read"]},
 {key:"development",label:"Development",roles:["Development"],types:["llm","code","sandbox+github_write"]},
 {key:"qa",label:"QA / Security",roles:["QA & Security"],types:["sandbox","review"]},
 {key:"review",label:"Final Review",roles:["Final Review"],types:["review"]},
] as const;

export default function DevWorkspacePage(){
 const params=useParams<{id:string}>();const router=useRouter();const id=String(params.id);
 const[team,setTeam]=useState<Team|null>(null);const[runs,setRuns]=useState<Run[]>([]);const[binding,setBinding]=useState<Binding|null>(null);const[readiness,setReadiness]=useState<TeamReadiness|null>(null);const[readinessLoading,setReadinessLoading]=useState(false);const[task,setTask]=useState("");const[budget,setBudget]=useState("100.00");const[busy,setBusy]=useState("");const[error,setError]=useState("");const[notice,setNotice]=useState("");
 const loadReadiness=async()=>{setReadinessLoading(true);try{const value=await api<TeamReadiness>(`/agent-teams/${id}/readiness/`);setReadiness(value);return value}catch(e){const fallback:TeamReadiness={ready:false,checks:{},blockers:[e instanceof Error?e.message:"Не удалось проверить готовность Dev Studio"],warnings:[],actions:[],models:[]};setReadiness(fallback);return fallback}finally{setReadinessLoading(false)}};
 const load=async()=>{try{const[t,r,ready]=await Promise.all([api<Team>(`/agent-teams/${id}/`),api<Run[]>(`/agent-runs/?team=${encodeURIComponent(id)}`),loadReadiness()]);setTeam(t);setRuns(r);setBudget(String(t.max_cost_rub_per_run||"100.00"));setReadiness(ready);if(t.project){try{setBinding(await api<Binding>(`/projects/${t.project}/github/`))}catch{setBinding(null)}}setError("")}catch(e){setError(e instanceof Error?e.message:"Не удалось загрузить Dev Project")}};
 useEffect(()=>{void load()},[id]);
 const latest=useMemo(()=>runs[0]||null,[runs]);
 const activeRun=useMemo(()=>runs.find(item=>["queued","planning","running","waiting_tool","waiting_approval","reviewing"].includes(item.state))||null,[runs]);
 useEffect(()=>{if(!latest||!["queued","planning","running","waiting_tool","reviewing"].includes(latest.state))return;const timer=window.setInterval(()=>void load(),2500);return()=>window.clearInterval(timer)},[latest?.state,id]);
 const run=async()=>{if(!team)return;setBusy("run");setError("");setNotice("");try{const fresh=await loadReadiness();if(!fresh.ready){setError("Dev Team пока не готова к запуску: "+fresh.blockers.join("; "));return}const started=await api<Run>(`/agent-teams/${id}/run/`,{method:"POST",body:JSON.stringify({objective:task.trim()||team.objective})});setTask("");router.push(`/app/runs/${started.id}`)}catch(e){setError(e instanceof Error?e.message:"Не удалось запустить задачу")}finally{setBusy("")}};
 const saveBudget=async()=>{if(!team)return;const value=Number(budget.replace(",","."));if(!Number.isFinite(value)||value<=0){setError("Лимит запуска должен быть больше нуля");return}setBusy("budget");setError("");setNotice("");try{const updated=await api<Team>(`/agent-teams/${id}/`,{method:"PATCH",body:JSON.stringify({max_cost_rub_per_run:value.toFixed(4)})});setTeam(updated);setBudget(String(updated.max_cost_rub_per_run));setNotice("Лимит одного запуска обновлён.");await loadReadiness()}catch(e){setError(e instanceof Error?e.message:"Не удалось изменить лимит")}finally{setBusy("")}};
 const toggleActive=async()=>{if(!team)return;if(team.active&&activeRun){setError("Сначала остановите активный запуск. После этого команду можно поставить на паузу.");return}setBusy("active");setError("");setNotice("");try{const updated=await api<Team>(`/agent-teams/${id}/`,{method:"PATCH",body:JSON.stringify({active:!team.active})});setTeam(updated);setNotice(updated.active?"Dev Team снова активна.":"Dev Team поставлена на паузу. Новые задачи не запускаются.");await loadReadiness()}catch(e){setError(e instanceof Error?e.message:"Не удалось изменить состояние команды")}finally{setBusy("")}};
 const stageState=(stage:(typeof stageDefs)[number])=>{if(!latest)return "not_started";const related=latest.steps.filter(step=>stage.roles.includes(step.title as never)||stage.types.includes(step.action_type as never));if(!related.length)return latest.state==="completed"?"completed":"not_started";if(related.some(step=>step.state==="failed"))return "failed";if(related.some(step=>["running","waiting_approval"].includes(step.state)))return "running";if(related.every(step=>step.state==="completed"))return "completed";return "pending"};
 if(!team)return <main style={{padding:32}}>{error||"Загрузка…"}</main>;
 const blocked=Boolean(readiness&&!readiness.ready);
 return <main style={{maxWidth:1220,margin:"0 auto",padding:"30px 20px 70px"}}>
  <header style={{display:"flex",justifyContent:"space-between",gap:18,alignItems:"flex-start",marginBottom:26}}><div><Link href="/app/dev" style={{textDecoration:"none",opacity:.65}}>← Dev Studio</Link><h1 style={{fontSize:36,margin:"10px 0 6px"}}>{team.name}</h1><div style={{opacity:.62}}>{binding?.full_name||"GitHub repository"} · руководитель {team.director_name} · {team.active?"активна":"пауза"}</div></div><div style={{display:"flex",gap:8}}>{latest&&<Link href={`/app/runs/${latest.id}`} style={{padding:"10px 13px",border:"1px solid #ddd",borderRadius:10,textDecoration:"none",color:"inherit"}}>Открыть последний run</Link>}</div></header>
  {notice&&<div style={{padding:12,border:"1px solid #7aa78b",borderRadius:11,marginBottom:14}}>{notice}</div>}{error&&<div style={{padding:12,border:"1px solid #cb747a",borderRadius:11,marginBottom:14}}>{error}</div>}
  <section style={{border:`1px solid ${blocked?"#d99":"#ddd"}`,borderRadius:18,padding:18,marginBottom:20}}><div style={{display:"flex",justifyContent:"space-between",gap:14,alignItems:"flex-start"}}><div><div style={{fontSize:12,opacity:.55,textTransform:"uppercase"}}>DEV STUDIO · PREFLIGHT</div><h2 style={{margin:"7px 0 8px"}}>{readinessLoading?"Проверяем готовность…":readiness?.ready?"Готово к безопасному запуску":"Нужна настройка"}</h2>{readiness?.ready?<p style={{margin:0,opacity:.7}}>System-модели, обязательные роли, GitHub write-path и sandbox готовы. Платные шаги можно запускать.</p>:<><p style={{margin:"0 0 8px",opacity:.7}}>Платный run не начнётся, пока обязательный контур не будет готов.</p><ul style={{margin:"6px 0 0",paddingLeft:20}}>{(readiness?.blockers||[]).map(item=><li key={item} style={{marginBottom:4}}>{item}</li>)}</ul></>}</div><button disabled={readinessLoading||!!busy} onClick={()=>void loadReadiness()} style={{padding:"9px 12px",border:"1px solid #ccc",borderRadius:9,background:"transparent",whiteSpace:"nowrap"}}>Проверить снова</button></div>{readiness?.models?.length?<div style={{display:"flex",gap:7,flexWrap:"wrap",marginTop:12}}>{readiness.models.map(item=><span key={`${item.role}:${item.model}`} style={{fontSize:12,border:"1px solid #ddd",borderRadius:999,padding:"5px 8px"}}>{item.role}: {item.model}</span>)}</div>:null}{readiness?.actions?.some(item=>item.href)&&<div style={{display:"flex",gap:12,flexWrap:"wrap",marginTop:12}}>{readiness.actions.filter(item=>item.href).map(item=><Link key={item.code} href={item.href||"#"} style={{fontSize:13}}>{item.label} →</Link>)}</div>}</section>
  <section style={{display:"grid",gridTemplateColumns:"repeat(4,minmax(0,1fr))",gap:12,marginBottom:22}}>{stageDefs.map((stage,index)=>{const state=stageState(stage);return <article key={stage.key} style={{border:"1px solid #ddd",borderRadius:16,padding:16}}><div style={{fontSize:12,opacity:.48}}>ЭТАП {index+1}</div><strong style={{display:"block",fontSize:18,margin:"6px 0"}}>{stage.label}</strong><span style={{fontSize:13,opacity:.65}}>{state==="completed"?"✓ Готово":state==="running"?"● В работе":state==="failed"?"Ошибка":state==="pending"?"Ожидает":"Не начат"}</span></article>})}</section>
  <div style={{display:"grid",gridTemplateColumns:"minmax(0,1.25fr) minmax(300px,.75fr)",gap:18}}>
   <section style={{display:"grid",gap:16}}>
    <article style={{border:"1px solid #ddd",borderRadius:18,padding:20}}><h2 style={{marginTop:0}}>Текущая задача</h2><p style={{whiteSpace:"pre-wrap",opacity:.76}}>{latest?.objective||team.objective}</p>{latest&&<div style={{display:"flex",gap:12,flexWrap:"wrap",fontSize:13,opacity:.62}}><span>Статус: <strong>{stateLabel[latest.state]||latest.state}</strong></span><span>Шагов: {latest.step_count}</span><span>Инструментов: {latest.tool_call_count}</span><span>Расход: {Number(latest.cost_actual_rub||0).toFixed(2)} ₽</span><span>Лимит: {Number(team.max_cost_rub_per_run||0).toFixed(2)} ₽</span></div>}{latest?.approvals?.some(item=>item.status==="pending")&&<div style={{marginTop:12,padding:12,border:"1px solid #d8b16a",borderRadius:11}}>Нужно подтверждение пользователя. <Link href={`/app/runs/${latest.id}`}>Открыть запрос →</Link></div>}</article>
    {latest?.steps?.length?<article style={{border:"1px solid #ddd",borderRadius:18,padding:20}}><h2 style={{marginTop:0}}>Ход разработки</h2><div style={{display:"grid",gap:9}}>{latest.steps.map(step=><div key={step.id} style={{display:"grid",gridTemplateColumns:"34px minmax(0,1fr) auto",gap:10,alignItems:"start",padding:"10px 0",borderBottom:"1px solid #eee"}}><div style={{width:30,height:30,borderRadius:999,border:"1px solid #bbb",display:"grid",placeItems:"center"}}>{step.sequence}</div><div><strong>{step.title}</strong><div style={{fontSize:12,opacity:.58,marginTop:3}}>{step.agent_name} · {stateLabel[step.state]||step.state}</div>{step.public_log&&<div style={{fontSize:13,opacity:.72,marginTop:5,maxHeight:74,overflow:"hidden"}}>{step.public_log}</div>}</div><span style={{fontSize:12,opacity:.55}}>{Number(step.cost_rub||0).toFixed(2)} ₽</span></div>)}</div></article>:null}
    <article style={{border:"1px solid #ddd",borderRadius:18,padding:20}}><h2 style={{marginTop:0}}>Новая задача проекту</h2><textarea rows={5} value={task} onChange={e=>setTask(e.target.value)} placeholder="Опишите следующий результат: исправить баг, добавить функцию, провести аудит..." style={{width:"100%",boxSizing:"border-box",padding:12,border:"1px solid #ccc",borderRadius:10,resize:"vertical"}}/><div style={{fontSize:12,opacity:.6,marginTop:8}}>Один активный запуск на команду. Жёсткий максимум расходов этого запуска: {Number(team.max_cost_rub_per_run||0).toFixed(2)} ₽.</div><button disabled={!!busy||readinessLoading||!readiness?.ready||!team.active||!!activeRun} onClick={()=>void run()} title={!readiness?.ready?(readiness?.blockers||[]).join("; "):""} style={{marginTop:10,padding:"11px 15px",border:0,borderRadius:10,fontWeight:700,opacity:readiness?.ready?1:.55}}>{busy==="run"?"Запускаем…":activeRun?"Команда уже работает":!readiness?.ready?"Сначала настройте Dev Studio":"Запустить команду"}</button></article>
   </section>
   <aside style={{display:"grid",gap:16,alignContent:"start"}}>
    <article style={{border:"1px solid #ddd",borderRadius:18,padding:20}}><h3 style={{marginTop:0}}>GitHub</h3>{binding?<><strong>{binding.full_name}</strong><div style={{fontSize:13,opacity:.62,marginTop:5}}>Default: {binding.default_branch}<br/>{binding.private?"Private repository":"Public repository"}<br/>{binding.write_enabled?"Запись разрешена":"Только чтение"}</div>{latest?.output_payload?.working_branch&&<div style={{marginTop:10,padding:9,border:"1px solid #ddd",borderRadius:9,fontSize:13}}>Рабочая ветка<br/><strong>{latest.output_payload.working_branch}</strong></div>}</>:<p style={{opacity:.65}}>Repository binding не найден.</p>}</article>
    <article style={{border:"1px solid #ddd",borderRadius:18,padding:20}}><h3 style={{marginTop:0}}>Безопасность и бюджет</h3><label style={{display:"grid",gap:6,fontSize:13}}>Максимум на один запуск, ₽<input inputMode="decimal" value={budget} onChange={e=>setBudget(e.target.value)} disabled={!!activeRun} style={{padding:10,border:"1px solid #ccc",borderRadius:9}}/></label><button disabled={!!busy||!!activeRun||budget===String(team.max_cost_rub_per_run)} onClick={()=>void saveBudget()} style={{marginTop:9,padding:"9px 12px",border:"1px solid #ccc",borderRadius:9,background:"transparent"}}>{busy==="budget"?"Сохраняем…":"Сохранить лимит"}</button><div style={{fontSize:12,opacity:.58,marginTop:10}}>Лимит нельзя менять во время активного запуска. Внутренние шаги дополнительно ограничены лимитами каждого агента.</div><button disabled={!!busy||(team.active&&!!activeRun)} onClick={()=>void toggleActive()} style={{marginTop:12,padding:"9px 12px",border:"1px solid #ccc",borderRadius:9,background:"transparent"}}>{busy==="active"?"Сохраняем…":team.active?"Поставить команду на паузу":"Возобновить команду"}</button></article>
    <article style={{border:"1px solid #ddd",borderRadius:18,padding:20}}><h3 style={{marginTop:0}}>Команда</h3>{team.members.map(member=><div key={member.id} style={{padding:"8px 0",borderBottom:"1px solid #eee"}}><strong>{member.role}</strong><div style={{fontSize:12,opacity:.58}}>{member.agent_name}{member.can_delegate?" · может делегировать":""}</div></div>)}</article>
    <article style={{border:"1px solid #ddd",borderRadius:18,padding:20}}><h3 style={{marginTop:0}}>История</h3>{runs.length?runs.slice(0,8).map(run=><Link key={run.id} href={`/app/runs/${run.id}`} style={{display:"block",padding:"8px 0",borderBottom:"1px solid #eee",textDecoration:"none",color:"inherit"}}><strong>{stateLabel[run.state]||run.state}</strong><div style={{fontSize:12,opacity:.55}}>{new Date(run.created_at).toLocaleString("ru-RU")} · {Number(run.cost_actual_rub||0).toFixed(2)} ₽</div></Link>):<p style={{opacity:.65}}>Запусков пока нет.</p>}</article>
   </aside>
  </div>
 </main>;
}
