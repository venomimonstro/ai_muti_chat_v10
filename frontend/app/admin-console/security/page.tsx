"use client";

import {FormEvent,useEffect,useState} from "react";
import {api} from "../../../lib/api";
import styles from "../admin.module.css";

type Event={id:string;category:string;severity:string;status:string;user_id:string|null;ip_address:string|null;summary:string;details:Record<string,unknown>;created_at:string;resolved_at:string|null};
export default function SecurityAdmin(){const[items,setItems]=useState<Event[]>([]);const[status,setStatus]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);const load=async()=>{try{setItems(await api<Event[]>(`/admin/security/?limit=200${status?`&status=${status}`:""}`));}catch(r){setError(r instanceof Error?r.message:"Не удалось загрузить события");}};useEffect(()=>{void load();},[status]);const act=async(id:string,action:"investigate"|"resolve"|"contain")=>{if(action==="contain"&&!window.confirm("Contain заблокирует связанного пользователя и отзовёт API keys. Продолжить?"))return;setBusy(true);try{await api(`/admin/security/${id}/action/`,{method:"POST",body:JSON.stringify({action})});await load();}catch(r){setError(r instanceof Error?r.message:"Действие не выполнено");}finally{setBusy(false);}};const create=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);setBusy(true);try{await api("/admin/security/",{method:"POST",body:JSON.stringify({category:f.get("category"),summary:f.get("summary"),severity:f.get("severity"),user_id:f.get("user_id")||null,ip_address:f.get("ip_address")||null})});e.currentTarget.reset();await load();}catch(r){setError(r instanceof Error?r.message:"Событие не создано");}finally{setBusy(false);}};return <><header className={styles.header}><div><h1>Безопасность</h1><p>Abuse, подозрительная активность и containment.</p></div></header>{error&&<div className={`${styles.notice} ${styles.error}`}>{error}</div>}<section className={styles.section}><h2>Создать событие</h2><form className={styles.filters} onSubmit={create}><input name="category" placeholder="category" required/><select name="severity" defaultValue="warning"><option value="info">info</option><option value="warning">warning</option><option value="critical">critical</option></select><input name="summary" placeholder="Краткое описание" required style={{minWidth:260}}/><input name="user_id" placeholder="user UUID (необязательно)"/><input name="ip_address" placeholder="IP (необязательно)"/><button className={`${styles.button} ${styles.primary}`} disabled={busy}>Создать</button></form></section><section className={styles.section}><div className={styles.filters}><select value={status} onChange={e=>setStatus(e.target.value)}><option value="">Все статусы</option><option value="open">open</option><option value="investigating">investigating</option><option value="resolved">resolved</option></select><button className={styles.button} onClick={()=>void load()}>Обновить</button></div><table className={styles.table}><thead><tr><th>Severity</th><th>Категория</th><th>Описание</th><th>Пользователь / IP</th><th>Статус</th><th>Дата</th><th></th></tr></thead><tbody>{items.map(item=><tr key={item.id}><td className={item.severity==="critical"?styles.bad:item.severity==="warning"?styles.warn:""}>{item.severity}</td><td>{item.category}</td><td>{item.summary}</td><td>{item.user_id??"—"}<br/><small>{item.ip_address??""}</small></td><td><span className={styles.pill}>{item.status}</span></td><td>{new Date(item.created_at).toLocaleString("ru")}</td><td><div className={styles.actions}>{item.status==="open"&&<button className={styles.button} disabled={busy} onClick={()=>void act(item.id,"investigate")}>Investigate</button>}{item.status!=="resolved"&&<button className={styles.button} disabled={busy} onClick={()=>void act(item.id,"resolve")}>Resolve</button>}{item.user_id&&item.status!=="resolved"&&<button className={`${styles.button} ${styles.danger}`} disabled={busy} onClick={()=>void act(item.id,"contain")}>Contain</button>}</div></td></tr>)}</tbody></table></section></>}
