"use client";

import {FormEvent,useEffect,useState} from "react";
import {api} from "../../../lib/api";
import styles from "../admin.module.css";

type Item={key:string;title:string;status:"pending"|"approved"|"rejected";evidence_reference:string;notes:string;reviewed_by:string|null;reviewed_at:string|null};
export default function ComplianceAdmin(){const[items,setItems]=useState<Item[]>([]);const[selected,setSelected]=useState<Item|null>(null);const[error,setError]=useState("");const[busy,setBusy]=useState(false);const load=async()=>{try{const data=await api<Item[]>("/admin/signoffs/");setItems(data);if(selected){setSelected(data.find(i=>i.key===selected.key)??null);}}catch(r){setError(r instanceof Error?r.message:"Не удалось загрузить compliance");}};useEffect(()=>{void load();},[]);const save=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();if(!selected)return;const f=new FormData(e.currentTarget);setBusy(true);setError("");try{await api("/admin/signoffs/",{method:"POST",body:JSON.stringify({key:selected.key,status:f.get("status"),evidence_reference:f.get("evidence_reference"),notes:f.get("notes")})});await load();}catch(r){setError(r instanceof Error?r.message:"Sign-off не сохранён");}finally{setBusy(false);}};const approved=items.filter(i=>i.status==="approved").length;return <><header className={styles.header}><div><h1>Compliance</h1><p>Юридические, платёжные и provider-specific sign-offs перед запуском.</p></div><span className={styles.pill}>{approved}/{items.length} approved</span></header>{error&&<div className={`${styles.notice} ${styles.error}`}>{error}</div>}<section className={styles.grid}><div className={styles.card}><small>Approved</small><strong className={approved===items.length?styles.good:styles.warn}>{approved}</strong></div><div className={styles.card}><small>Pending</small><strong>{items.filter(i=>i.status==="pending").length}</strong></div><div className={styles.card}><small>Rejected</small><strong className={items.some(i=>i.status==="rejected")?styles.bad:""}>{items.filter(i=>i.status==="rejected").length}</strong></div><div className={styles.card}><small>Launch state</small><strong>{items.length>0&&approved===items.length?"READY":"BLOCKED"}</strong></div></section><section className={styles.two}><div className={styles.section}><h2>Обязательные проверки</h2><table className={styles.table}><tbody>{items.map(item=><tr key={item.key}><td><button className={styles.button} onClick={()=>setSelected(item)}>{item.title}</button><br/><small>{item.key}</small></td><td><span className={styles.pill}>{item.status}</span></td><td>{item.evidence_reference||"—"}</td></tr>)}</tbody></table></div><div className={styles.section}><h2>{selected?selected.title:"Выберите проверку"}</h2>{selected?<form onSubmit={save} style={{display:"grid",gap:12}}><label>Статус<select name="status" defaultValue={selected.status} style={{display:"block",width:"100%",marginTop:5}}><option value="pending">pending</option><option value="approved">approved</option><option value="rejected">rejected</option></select></label><label>Evidence reference<input name="evidence_reference" defaultValue={selected.evidence_reference} placeholder="URL, номер договора, хэш отчёта" style={{display:"block",width:"100%",marginTop:5}}/></label><label>Notes<textarea name="notes" defaultValue={selected.notes} rows={6} style={{display:"block",width:"100%",marginTop:5}}/></label><button className={`${styles.button} ${styles.primary}`} disabled={busy}>Сохранить</button>{selected.reviewed_at&&<small>Последняя проверка: {new Date(selected.reviewed_at).toLocaleString("ru")}</small>}</form>:<p>Approval без evidence backend не принимает. Провайдеры, включённые в production, автоматически получают отдельный sign-off.</p>}</div></section></>}
