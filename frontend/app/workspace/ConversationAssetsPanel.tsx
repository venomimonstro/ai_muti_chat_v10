"use client";

import {useCallback,useEffect,useState} from "react";
import {api} from "../../lib/api";

type Kind="images"|"files"|"links";
type ImageItem={id:string;prompt:string;model_name:string;created_at:string;images:Array<{id:string;source_url:string;mime_type:string}>};
type FileItem={id:string;original_name:string;detected_type:string;status:string;size_bytes:number};
type LinkItem={url:string;title:string;message_id:string|null;source:string};
type AssetsResponse={counts:{images:number;files:number;links:number};images:ImageItem[];files:FileItem[];links:LinkItem[]};

export function ConversationAssetsPanel({conversationId}:{conversationId:string}){
 const[open,setOpen]=useState(false);const[kind,setKind]=useState<Kind>("images");const[query,setQuery]=useState("");const[data,setData]=useState<AssetsResponse|null>(null);const[busy,setBusy]=useState(false);const[error,setError]=useState("");
 const load=useCallback(async()=>{if(!open)return;setBusy(true);setError("");try{const suffix=query.trim()?`&q=${encodeURIComponent(query.trim())}`:"";const response=await api<AssetsResponse>(`/conversations/${conversationId}/assets/?kind=${kind}${suffix}`);setData(response);}catch(reason){setError(reason instanceof Error?reason.message:"Не удалось загрузить материалы чата");}finally{setBusy(false);}},[conversationId,kind,open,query]);
 useEffect(()=>{if(!open)return;const timer=window.setTimeout(()=>void load(),query?250:0);return()=>window.clearTimeout(timer);},[load,open,query]);
 useEffect(()=>{setOpen(false);setData(null);setQuery("");},[conversationId]);
 return <><button type="button" className="olderButton" onClick={()=>setOpen(true)} style={{position:"sticky",top:8,zIndex:3,margin:"8px auto",display:"block"}}>Материалы чата</button>{open&&<div className="overlayBackdrop" onMouseDown={event=>{if(event.target===event.currentTarget)setOpen(false)}}><section className="searchOverlay" role="dialog" aria-modal="true" aria-label="Материалы чата" style={{maxWidth:900}}><div style={{display:"flex",gap:8,alignItems:"center",padding:12,flexWrap:"wrap"}}><b style={{marginRight:"auto"}}>Материалы этого чата</b><a className="olderButton" href={`/app/images?conversation=${encodeURIComponent(conversationId)}`}>Создать изображение</a>{(["images","files","links"] as Kind[]).map(value=><button key={value} type="button" className="olderButton" onClick={()=>{setKind(value);setData(null)}} aria-pressed={kind===value}>{value==="images"?`Изображения${data?` · ${data.counts.images}`:""}`:value==="files"?`Файлы${data?` · ${data.counts.files}`:""}`:`Ссылки${data?` · ${data.counts.links}`:""}`}</button>)}<button type="button" className="iconButton" onClick={()=>setOpen(false)} aria-label="Закрыть">×</button></div><div className="searchBox" style={{margin:"0 12px 12px"}}><input value={query} onChange={event=>setQuery(event.target.value)} placeholder={kind==="images"?"Поиск по промпту или модели":kind==="files"?"Поиск по имени файла":"Поиск по ссылкам"}/></div><div className="searchResults" style={{maxHeight:"65vh",overflow:"auto"}}>{busy&&<p className="mutedState">Загружаем…</p>}{error&&<p className="inlineError">{error}</p>}{!busy&&!error&&data&&kind==="images"&&data.images.length===0&&<p className="mutedState">В этом чате пока нет изображений.</p>}{kind==="images"&&data?.images.map(generation=><article key={generation.id} style={{padding:12,borderBottom:"1px solid var(--border, #ddd)"}}><div style={{display:"flex",justifyContent:"space-between",gap:12}}><b>{generation.model_name}</b><small>{new Date(generation.created_at).toLocaleString("ru-RU")}</small></div><p style={{margin:"6px 0 10px"}}>{generation.prompt}</p><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fill,minmax(160px,1fr))",gap:8}}>{generation.images.map(image=><a key={image.id} href={image.source_url} target="_blank" rel="noreferrer"><img src={image.source_url} alt={generation.prompt} loading="lazy" style={{width:"100%",aspectRatio:"1",objectFit:"cover",borderRadius:10}}/></a>)}</div></article>)}{!busy&&!error&&data&&kind==="files"&&data.files.length===0&&<p className="mutedState">В этом чате пока нет файлов.</p>}{kind==="files"&&data?.files.map(file=><div key={file.id} className="searchResult" style={{display:"block",cursor:"default"}}><b>{file.original_name}</b><span>{file.detected_type} · {file.status} · {(file.size_bytes/1024).toFixed(1)} КБ</span></div>)}{!busy&&!error&&data&&kind==="links"&&data.links.length===0&&<p className="mutedState">В этом чате пока нет ссылок.</p>}{kind==="links"&&data?.links.map(link=><a key={link.url} className="searchResult" href={link.url} target="_blank" rel="noopener noreferrer" style={{display:"block"}}><b>{link.title||link.url}</b><span style={{wordBreak:"break-all"}}>{link.url}</span></a>)}</div></section></div>}</>;
}
